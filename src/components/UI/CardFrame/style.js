import styled from "@emotion/styled";

import { Box } from "rebass/styled-components";

const CardFrameContainer = styled(Box)`
  .ant-card-body {
    padding: 5% 5% 5% 0;
  }
`;

export { CardFrameContainer };
