import styled from "@emotion/styled";
import { Box } from "rebass";

const GdprContainer = styled(Box)`
  position: absolute;
  top: 30rem;
  right: 5rem;
  background: white;
  font-family: Manrope;
  font-style: normal;
  border: 1px solid lightgray;
  border-radius: 5px;
`;

export { GdprContainer };
